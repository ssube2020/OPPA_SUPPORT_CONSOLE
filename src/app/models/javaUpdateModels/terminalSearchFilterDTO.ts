export interface TerminalSearchFilterDTO{
        checkBox? : boolean,
        currentContent?: string,
        currentSoft?: string,  
        terminalName?: string, 
        contentName?: string,
        updateenabled?: string,
        offset? : number,
        perPage? : number
    }